// (a) 为枚举声明标记，此枚举的值表示一周中的 7 天。
enum week_day { MON, TUE, WEN, THU, FRI, SAT, SUN };
// (b) 用 typedef 定义(a)中枚举的名字。
typedef enum { MON, TUE, WEN, THU, FRI, SAT, SUN } week_day;

// 书作者答案链接：http://knking.com/books/c2/answers/c16.html